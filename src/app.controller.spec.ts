import { beforeEach, describe, it } from 'node:test';

describe('AppController', () => {
  beforeEach(async () => {
  });
  describe('root', () => {
    it('should return "Hello World!"', () => {
    });
  });
});

